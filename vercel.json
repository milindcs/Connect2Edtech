{
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm install --omit=optional --include=dev",
  "outputDirectory": "frontend/dist",
  "rootDirectory": ".",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
