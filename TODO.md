- [ ] Update render.yaml to start backend directly (avoid missing root server.js issues)
- [ ] Verify backend server starts locally via workspace (npm start in backend)
- [ ] (If needed) rebuild frontend and ensure backend serves dist correctly

